documentation_complete: true

prodtype: sle15

title: 'Install apparmor-parser Package'

description: |-
    {{{ describe_package_install(package="apparmor-parser") }}}

rationale: |-
    AppArmor is a Mandatory Access Control (MAC) system which is a kernel (LSM) 
    enhancement to confine programs to a limited set of resources. 

    The <tt>apparmor-parser</tt> package contains the profile parser for AppArmor.

severity: high

references:
    cis@sle15: 1.7.1.1.2

ocil_clause: 'the package is not installed'

ocil: '{{{ ocil_package(package="apparmor-parser") }}}'

template:
    name: package_installed
    vars:
        pkgname: apparmor-parser
