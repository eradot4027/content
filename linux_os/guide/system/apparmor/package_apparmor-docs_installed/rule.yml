documentation_complete: true

prodtype: sle15

title: 'Install apparmor-docs Package'

description: |-
    {{{ describe_package_install(package="apparmor-docs") }}}

rationale: |-
    AppArmor is a Mandatory Access Control (MAC) system which is a kernel (LSM) 
    enhancement to confine programs to a limited set of resources. 

    The <tt>apparmor-docs</tt> package contains the core documentation of the AppArmor.

severity: high

references:
    cis@sle15: 1.7.1.1.1

ocil_clause: 'the package is not installed'

ocil: '{{{ ocil_package(package="apparmor-docs") }}}'

template:
    name: package_installed
    vars:
        pkgname: apparmor-docs
